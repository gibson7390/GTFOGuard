PROJECT RESET AND ARCHITECTURE LOCK

You are acting as a senior software architect and implementation engineer for GTFOGuard.

IMPORTANT:

You may analyze, recommend, create files, modify files, and remove files ONLY within the scope defined below.

You may NOT:
- Push to GitHub
- Commit changes
- Publish anything
- Create new architecture decisions
- Convert the project into a web application
- Create Flask
- Create FastAPI
- Create APIs
- Create HTML/CSS/JavaScript dashboards
- Create Docker files
- Create CI/CD pipelines
- Add machine learning
- Add cloud services

without explicit approval.

--------------------------------------------------
PHASE 1: ANALYZE CURRENT STATE
--------------------------------------------------

First:

1. Scan the entire repository.
2. Produce a complete file tree.
3. Identify every file that was generated automatically.
4. Explain the purpose of each file.
5. Mark files as:
   - KEEP
   - REMOVE
   - REVIEW

Do not modify anything yet.

Wait for approval.

--------------------------------------------------
PHASE 2: CLEANUP
--------------------------------------------------

After approval:

Remove all files that belong to the incorrect Flask/web architecture.

Return the repository to a clean Python package state.

--------------------------------------------------
PHASE 3: CREATE APPROVED ARCHITECTURE
--------------------------------------------------

The approved architecture is:

GTFOGuard/
│
├── README.md
├── requirements.txt
│
├── gtfoguard/
│   ├── init.py
│   ├── main.py
│   ├── config.py
│   ├── models.py
│   │
│   ├── collectors/
│   │   └── init.py
│   │
│   ├── detectors/
│   │   └── init.py
│   │
│   ├── intelligence/
│   │   └── init.py
│   │
│   ├── ui/
│   │   └── init.py
│   │
│   └── utils/
│       └── init.py

--------------------------------------------------
PHASE 4: CREATE README
--------------------------------------------------

Create a professional GitHub README that includes:

- Project title
- Description
- Why GTFOGuard exists
- Key features
- Planned architecture
- Installation instructions
- Usage instructions
- Roadmap
- License section

Describe GTFOGuard as:

"A behavioral Linux security monitoring framework focused on detecting suspicious Living-Off-The-Land (LOTL) activity, GTFOBins abuse, process ancestry anomalies, and explainable risk scoring."

Do not claim features that do not exist.

Only document implemented functionality and clearly label future plans.

--------------------------------------------------
PHASE 5: MINIMAL WORKING STARTER
--------------------------------------------------

Create only the minimum code required to allow:

python -m gtfoguard.main

to successfully run and print:

GTFOGuard starting...

No detection engine yet.

No dashboard yet.

No GTFOBins fetching yet.

No scoring engine yet.

No additional functionality unless approved.

--------------------------------------------------
FINAL REQUIREMENT
--------------------------------------------------

At the end of all work:

Provide:
1. Final file tree
2. Summary of every created file
3. Summary of every removed file
4. List of recommended next steps

Do NOT commit.
Do NOT push.
Wait for approval before any Git operations.