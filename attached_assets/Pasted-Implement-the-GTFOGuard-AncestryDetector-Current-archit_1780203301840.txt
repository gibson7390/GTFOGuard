Implement the GTFOGuard AncestryDetector.

Current architecture:

ProcessCollector
→ GTFOBinsDetector
→ CommandLineDetector
→ PathAnomalyDetector
→ RiskScorer
→ Display

Goal:

Add parent-child process relationship detection while preserving the existing architecture and separation of concerns.

Requirements:

1. Create a dedicated AncestryDetector.
2. The detector must analyze parent-child process relationships.
3. The detector must emit DetectionResult objects.
4. The detector must not perform scoring.
5. The detector must not perform display logic.
6. The detector must not modify process collection behavior.
7. Maintain separation of concerns throughout.

Architecture requirements:

GOOD:

snapshots
→ AncestryDetector.detect()
→ List[DetectionResult]

BAD:

AncestryDetector assigns severity

BAD:

AncestryDetector assigns scores

BAD:

AncestryDetector renders output

All risk classification must remain inside RiskScorer.

Detection scope:

Create detections for suspicious parent-child combinations.

Examples:

apache2 → sh
apache2 → bash
apache2 → python

nginx → sh
nginx → bash
nginx → python

cron → curl → sh

sshd → find

The detector may begin with a small hardcoded rule set.

The detector should focus on process ancestry relationships only.

Detection output requirements:

Each detection must include:

- pid
- process name
- parent process name
- detection_type
- human-readable reason

Use a dedicated detection_type:

ancestry_anomaly

Engine integration requirements:

Add AncestryDetector into the existing Engine pipeline.

Desired flow:

ProcessCollector
→ GTFOBinsDetector
→ CommandLineDetector
→ PathAnomalyDetector
→ AncestryDetector
→ RiskScorer
→ Display

The Engine remains coordinator-only.

The Engine must not implement ancestry logic.

Validation requirements:

Validate with:

1. Real process collection.
2. Synthetic parent-child chains.
3. Benign parent-child chains.
4. Full Engine execution.

Show:

- Raw ancestry relationships tested.
- Detector output.
- Engine output before scoring.
- RiskScorer output after scoring.
- Exact console output.

Code style requirements:

- Production-quality Python.
- Small focused files.
- Self-explanatory names.
- No AI-style comments.
- No placeholder comments.
- No detector logic inside Engine.
- No scoring logic inside detector.

Restrictions:

- No correlation engine.
- No monitoring loop.
- No network detector.
- No UI redesign.
- No detector rewrites.
- No scoring redesign.
- No commits.
- No pushes.

Deliver:

1. Architecture summary
2. Files created/modified
3. Validation results
4. Exact console output
5. Any discovered tradeoffs

Implement only the AncestryDetector phase.